import { TestBed } from '@angular/core/testing';
import { StateOperations } from '../internal/internals';
import { InternalDispatcher } from '../internal/dispatcher';
import { StateStream } from './state-stream';
import { NgxsConfig } from '../symbols';
/**
 * State Context factory class
 * @ignore
 */
export declare class InternalStateOperations {
    private _stateStream;
    private _dispatcher;
    private _config;
    private _testBed;
    constructor(_stateStream: StateStream, _dispatcher: InternalDispatcher, _config: NgxsConfig, _testBed: TestBed);
    /**
     * Returns the root state operators.
     */
    getRootStateOperations(): StateOperations<any>;
    private verifyDevMode;
    private ensureStateAndActionsAreImmutable;
}
